import React, { lazy, useEffect, useState } from 'react'

const Loader = () => {
  // const [pageUrl, setPageUrl] = useState('')
  // useEffect(()=> {
  //   const handlePageUrl = lazy(() => {
  // return new Promise((resolve) => {
  //   setTimeout(() => resolve(import(setPageUrl)), 300)
  // })
  //   }
  // )
  // return pageUrl
}

export default Loader
